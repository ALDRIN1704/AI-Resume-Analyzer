# ResumeIQ — AI Resume Analyzer

![Java](https://img.shields.io/badge/Java-21-orange)
![Spring Boot](https://img.shields.io/badge/Spring%20Boot-4.1.1-brightgreen)
![Python](https://img.shields.io/badge/Python-FastAPI-blue)
![AI](https://img.shields.io/badge/AI-Groq-red)
![Deployment](https://img.shields.io/badge/Deployment-Render-purple)

**AI-powered resume analysis with deterministic Java scoring, semantic requirement matching, targeted suggestions, and cover-letter generation.**

---

## 🚀 Live Deployment

### ResumeIQ Application

**Live Application**

https://ai-resume-analyzer-1-g2l2.onrender.com/

### FastAPI AI Service

**Health Check**

https://ai-resume-analyzer-uazs.onrender.com/health

**Swagger API Documentation**

https://ai-resume-analyzer-uazs.onrender.com/docs

**OpenAPI Specification**

https://ai-resume-analyzer-uazs.onrender.com/openapi.json

### Production Services

| Service | Method | URL |
|---|---|---|
| ResumeIQ Web Application | `GET` | https://ai-resume-analyzer-1-g2l2.onrender.com/ |
| Spring Boot Analysis API | `POST` | https://ai-resume-analyzer-1-g2l2.onrender.com/api/analyze |
| FastAPI Health | `GET` | https://ai-resume-analyzer-uazs.onrender.com/health |
| FastAPI Swagger | `GET` | https://ai-resume-analyzer-uazs.onrender.com/docs |
| FastAPI OpenAPI | `GET` | https://ai-resume-analyzer-uazs.onrender.com/openapi.json |
| AI Structured Analysis | `POST` | https://ai-resume-analyzer-uazs.onrender.com/analyze |
| Semantic Requirement Matching | `POST` | https://ai-resume-analyzer-uazs.onrender.com/match-requirements |
| Suggestions & Cover Letter | `POST` | https://ai-resume-analyzer-uazs.onrender.com/generate |

> **Note:** POST endpoints require request bodies. Test them through the ResumeIQ application, Postman, or FastAPI Swagger.

---

# 📌 Overview

**ResumeIQ** is a full-stack AI Resume Analyzer that compares a candidate's resume against a target job description.

The application accepts:

- PDF or DOCX resume
- Target job description

It extracts structured information from the resume and job description, identifies matched and missing skills, maps job requirements to evidence in the resume, calculates compatibility scores, generates targeted improvement suggestions, and produces a tailored cover letter.

The application follows a hybrid architecture:

- **Java / Spring Boot** handles document processing, orchestration, deterministic matching, scoring, and the web application.
- **Python / FastAPI** provides the AI/NLP service.
- **Groq-hosted LLM inference** performs structured extraction, semantic requirement analysis, suggestions, and cover-letter generation.
- **HTML, CSS, and JavaScript** provide the frontend dashboard.

The final Skill Match and ATS Compatibility scores are calculated using deterministic Java logic rather than being directly generated by the LLM.

---

# ✨ Key Features

| Feature | Description |
|---|---|
| Resume Upload | Supports PDF and DOCX resumes |
| File Validation | Validates resume format and empty uploads |
| Resume Parsing | Apache PDFBox for PDF and Apache POI for DOCX |
| Text Cleaning | Normalizes extracted text while preserving technical terms |
| AI Resume Extraction | Extracts structured resume information |
| Job Description Analysis | Extracts required skills, preferred skills, requirements and priorities |
| Skill Matching | Deterministic Java comparison of required skills |
| Semantic Matching | Maps non-skill job requirements to resume evidence |
| Skill Match Score | Calculates required technical skill coverage |
| ATS Compatibility | Weighted compatibility score calculated in Java |
| Evidence Mapping | Shows evidence supporting matched requirements |
| Gap Detection | Identifies missing skills and requirements |
| Improvement Suggestions | Generates JD-specific resume improvement suggestions |
| Cover Letter | Generates a tailored cover letter |
| Responsive Dashboard | Displays the complete analysis report |
| REST APIs | Spring Boot and FastAPI communicate using HTTP/JSON |
| Cloud Deployment | Spring Boot and FastAPI are deployed on Render |

---

# 🖼️ Application Preview

## ResumeIQ Dashboard

![ResumeIQ Dashboard](docs/images/dashboard.png)

The dashboard allows the user to upload a PDF/DOCX resume and provide the job description they want to target.

## Analysis Report

![ResumeIQ Analysis Report](docs/images/analysis-report.png)

The generated report displays Skill Match and ATS Compatibility independently, followed by requirement evidence, improvement suggestions, and a tailored cover letter.

---

# 🏗️ System Architecture

```text
Resume PDF / DOCX + Job Description
                  |
                  v
          Spring Boot Backend
                  |
                  v
          Resume Validation
                  |
          +-------+-------+
          |               |
          v               v
       PDFBox         Apache POI
         PDF              DOCX
          |               |
          +-------+-------+
                  |
                  v
           Extracted Text
                  |
                  v
           Text Cleaning
                  |
                  v
          FastAPI AI Service
                  |
          +-------+-------+
          |               |
          v               v
    Resume Analysis    JD Analysis
          |               |
          +-------+-------+
                  |
                  v
         Java Matching Engine
          +-------+--------+
          |                |
          v                v
    Skill Matching   Evidence Matching
          |                |
          +--------+-------+
                   |
                   v
          Java Scoring Engine
          +--------+--------+
          |                 |
          v                 v
      Skill Match      ATS Compatibility
          |                 |
          +--------+--------+
                   |
                   v
            AI Suggestions
                   |
                   v
            Cover Letter
                   |
                   v
          ResumeIQ Dashboard
```

---

# 🔄 Processing Pipeline

ResumeIQ processes a request through the following stages:

```text
1. Resume Upload
        |
        v
2. File Validation
        |
        v
3. PDF / DOCX Parsing
        |
        v
4. Text Cleaning
        |
        v
5. AI Resume + JD Extraction
        |
        v
6. Java Skill Matching
        |
        v
7. Semantic Requirement Matching
        |
        v
8. Java Compatibility Scoring
        |
        v
9. AI Improvement Suggestions
        |
        v
10. Tailored Cover Letter
        |
        v
11. ResumeIQ Dashboard
```

---

# 🛠️ Technology Stack

## Backend

- Java 21
- Spring Boot 4.1.1
- Spring Web MVC
- Jakarta Validation
- Maven
- Java HttpClient

## Document Processing

- Apache PDFBox 3.0.5
- Apache POI OOXML 5.4.1

## AI / NLP Service

- Python
- FastAPI
- Pydantic
- Uvicorn
- Groq API
- `openai/gpt-oss-120b`

## Frontend

- HTML5
- CSS3
- Vanilla JavaScript
- Fetch API

## Deployment

- Docker
- Render
- Environment Variables

## Development & Testing

- Eclipse IDE
- VS Code / PyCharm
- Postman
- FastAPI Swagger / OpenAPI
- Git
- GitHub

---

# 🔨 Development Approach

The application was developed incrementally using a **phase-by-phase checkpoint approach**.

Each major component was implemented and tested independently before being connected to the complete pipeline.

---

## Phase 1 — Resume Parsing

The first phase focused on reliable document extraction.

### PDF

PDF resumes are processed using:

```text
Apache PDFBox
```

### DOCX

DOCX resumes are processed using:

```text
Apache POI
```

### Checkpoint

```text
Resume
   |
   v
File Validation
   |
   v
PDF / DOCX Parser
   |
   v
Readable Resume Text
```

Image-based or scanned PDFs without extractable text return a clear message instead of silently continuing with empty content.

---

## Phase 2 — Resume Text Cleaning

Extracted text is normalized before being passed to the AI service.

The cleaning stage handles:

- Windows and Unix line endings
- Non-breaking spaces
- Repeated spaces
- Excessive blank lines
- Leading and trailing whitespace

Technical punctuation is preserved because terms such as:

```text
C++
C#
.NET
Node.js
CGPA
```

may contain important resume information.

### Checkpoint

```text
Raw Resume Text
       |
       v
TextCleaningService
       |
       v
Clean Resume Text
```

---

## Phase 3 — Structured AI Extraction

The cleaned resume and job description are sent from Spring Boot to FastAPI.

### Endpoint

```http
POST /analyze
```

The AI converts unstructured resume and job-description text into predictable structured data.

### Resume Information

The extracted resume information includes:

```text
Skills
Experience
Projects
Education
Certifications
Achievements
Research
Leadership
Activities
```

Example structure:

```json
{
  "skills": [],
  "experience": [],
  "projects": [],
  "education": [],
  "certifications": [],
  "achievements": [],
  "research": [],
  "leadership": [],
  "activities": []
}
```

### Job Information

The job description is separated into:

```text
Required Skills
Preferred Skills
Requirements
```

Each job requirement contains:

```text
Name
Category
Priority
```

Supported categories include:

```text
SKILL
EXPERIENCE
PROJECT
EDUCATION
CERTIFICATION
RESEARCH
ACHIEVEMENT
LEADERSHIP
ACTIVITY
OTHER
```

Requirement priorities include:

```text
REQUIRED
PREFERRED
OPTIONAL
```

### Checkpoint

```text
Resume Text + Job Description
              |
              v
        FastAPI /analyze
              |
              v
     Structured Resume Data
              +
       Structured JD Data
```

---

## Phase 4A — Deterministic Skill Matching

Required technical skills are matched in Java instead of asking the LLM to assign the final skill score.

For every required skill:

```text
JD Required Skill
       |
       v
    Normalize
       |
       v
Compare with Resume Skills
       |
       v
Matched / Missing
```

### Skill Match Formula

```text
Skill Match % =
Matched Required Skills
----------------------- x 100
Total Required Skills
```

Example:

```text
Matched Required Skills = 7
Total Required Skills   = 13

Skill Match = 7 / 13 x 100
            = 53.85%
```

The dashboard displays both the matched count and percentage.

---

## Phase 4B — Semantic Requirement Matching

Simple string comparison is not sufficient for broader requirements such as:

```text
Experience building backend applications
Experience working in Agile teams
Relevant software projects
Degree in Information Technology
```

FastAPI therefore performs semantic evidence matching.

### Endpoint

```http
POST /match-requirements
```

Example result:

```json
{
  "requirement": "Backend or full-stack software projects",
  "category": "PROJECT",
  "priority": "REQUIRED",
  "matched": true,
  "evidence": "Resume evidence supporting the requirement"
}
```

When supporting evidence is not found:

```json
{
  "matched": false,
  "evidence": ""
}
```

This allows ResumeIQ to explain why a requirement was considered matched instead of displaying only a percentage.

---

## Phase 5 — ATS Compatibility Scoring

The ATS Compatibility score is calculated deterministically in Java.

The LLM does **not** choose the final score.

Requirements receive weights based on priority:

| Priority | Weight |
|---|---:|
| Required | 3 |
| Preferred | 2 |
| Optional | 1 |

### Formula

```text
ATS Compatibility =
Earned Weighted Points
---------------------- x 100
Total Weighted Points
```

Example:

```text
Earned Points = 15
Total Points  = 34

ATS Compatibility = 15 / 34 x 100
                  = 44.12%
```

### Why Two Scores?

ResumeIQ deliberately separates:

```text
Skill Match
```

from:

```text
ATS Compatibility
```

**Skill Match** measures required technical skill coverage.

**ATS Compatibility** evaluates broader weighted job requirements, including skills, experience, education, projects, certifications, and other relevant criteria.

> **Note:** ResumeIQ's ATS Compatibility score is an application-defined compatibility metric. It does not claim to reproduce the internal ranking algorithm of any commercial Applicant Tracking System.

---

## Phase 6 — AI Suggestions and Cover Letter

Once deterministic matching and scoring are complete, the results are passed back to the AI service.

### Endpoint

```http
POST /generate
```

Two outputs are generated.

### Improvement Suggestions

Suggestions focus on:

- Missing requirements
- Weakly communicated experience
- JD-specific keywords
- Project descriptions
- Technical evidence
- Resume clarity

The system is designed not to recommend fabricating qualifications.

For genuinely missing skills, the candidate can be advised to learn the technology or include it only if they actually possess that experience.

### Tailored Cover Letter

The cover letter is generated from information extracted from the resume.

It is designed to:

- Highlight relevant experience
- Emphasize matching projects
- Reference relevant technical skills
- Align with the target role
- Avoid intentionally inventing missing qualifications

---

## Phase 7 — Frontend Dashboard

The complete backend pipeline is connected to a responsive frontend served directly by Spring Boot.

The result follows this hierarchy:

```text
Scores
  |
  v
Skills
  |
  v
Requirement Evidence
  |
  v
Improvement Suggestions
  |
  v
Tailored Cover Letter
```

### Requirement Evidence

![Requirement Analysis](docs/images/requirement-analysis.png)

Instead of displaying only a percentage, ResumeIQ shows the resume evidence associated with job requirements.

### Improvement Suggestions

![Resume Improvement Suggestions](docs/images/suggestions.png)

### Tailored Cover Letter

![Tailored Cover Letter](docs/images/cover-letter.png)

The generated letter can be copied directly from the dashboard.

---

# 🔌 API Design

The application consists of two communicating services.

---

## Spring Boot API

### Analyze Resume

```http
POST /api/analyze
```

Production URL:

```text
https://ai-resume-analyzer-1-g2l2.onrender.com/api/analyze
```

Content type:

```text
multipart/form-data
```

Parameters:

| Parameter | Type | Description |
|---|---|---|
| `resume` | File | PDF or DOCX resume |
| `jobDescription` | Text | Target job description |

The Spring Boot API orchestrates the complete analysis pipeline and returns the final analysis result.

---

# 🐍 FastAPI AI Service

Production base URL:

```text
https://ai-resume-analyzer-uazs.onrender.com
```

The Python service exposes the following endpoints:

| Method | Endpoint | Purpose |
|---|---|---|
| `GET` | `/health` | AI service health check |
| `POST` | `/analyze` | Resume and JD structured extraction |
| `POST` | `/match-requirements` | Semantic evidence matching |
| `POST` | `/generate` | Suggestions and cover-letter generation |
| `GET` | `/docs` | Swagger API documentation |
| `GET` | `/openapi.json` | OpenAPI specification |

### Swagger

https://ai-resume-analyzer-uazs.onrender.com/docs

### Health Check

https://ai-resume-analyzer-uazs.onrender.com/health

---

# 🌐 Production Endpoint Checkpoints

## Spring Boot / Java

### Application

```text
GET
https://ai-resume-analyzer-1-g2l2.onrender.com/
```

### Complete Analysis

```text
POST
https://ai-resume-analyzer-1-g2l2.onrender.com/api/analyze
```

---

## FastAPI / Python

### Health

```text
GET
https://ai-resume-analyzer-uazs.onrender.com/health
```

### Swagger

```text
GET
https://ai-resume-analyzer-uazs.onrender.com/docs
```

### OpenAPI

```text
GET
https://ai-resume-analyzer-uazs.onrender.com/openapi.json
```

### Structured Analysis

```text
POST
https://ai-resume-analyzer-uazs.onrender.com/analyze
```

### Semantic Requirement Matching

```text
POST
https://ai-resume-analyzer-uazs.onrender.com/match-requirements
```

### Suggestions & Cover Letter

```text
POST
https://ai-resume-analyzer-uazs.onrender.com/generate
```

---

# 🔗 Java ↔ Python Integration

Spring Boot communicates with FastAPI over HTTP.

```text
Spring Boot
    |
    | JSON / HTTP
    v
FastAPI
    |
    | Groq API
    v
AI Model
```

Java uses `HttpClient` with HTTP/1.1 for communication with the Python service.

### Local Environment

```text
Spring Boot : http://localhost:8081
FastAPI     : http://127.0.0.1:8000
```

### Production Environment

```text
Spring Boot
https://ai-resume-analyzer-1-g2l2.onrender.com

FastAPI
https://ai-resume-analyzer-uazs.onrender.com
```

The FastAPI production URL is provided to Spring Boot using:

```env
AI_SERVICE_URL=https://ai-resume-analyzer-uazs.onrender.com
```

---

# 📁 Project Structure

```text
AI-Resume-Analyzer/
│
├── resume-analyzer/
│   │
│   ├── src/main/java/com/project/resume_analyzer/
│   │   │
│   │   ├── controller/
│   │   │   └── ResumeController.java
│   │   │
│   │   ├── dto/
│   │   │   ├── AIAnalysisRequest.java
│   │   │   ├── AIAnalysisResponse.java
│   │   │   ├── AnalysisResult.java
│   │   │   ├── ATSScoreResult.java
│   │   │   ├── GenerationResponse.java
│   │   │   ├── JobAnalysis.java
│   │   │   ├── JobRequirement.java
│   │   │   ├── MatchResult.java
│   │   │   ├── RequirementMatch.java
│   │   │   ├── RequirementMatchResponse.java
│   │   │   └── ResumeAnalysis.java
│   │   │
│   │   ├── service/
│   │   │   ├── AIService.java
│   │   │   ├── MatchingService.java
│   │   │   ├── ResumeParserService.java
│   │   │   ├── ScoringService.java
│   │   │   └── TextCleaningService.java
│   │   │
│   │   └── ResumeAnalyzerApplication.java
│   │
│   ├── src/main/resources/
│   │   ├── static/
│   │   │   ├── css/
│   │   │   ├── js/
│   │   │   └── index.html
│   │   │
│   │   └── application.properties
│   │
│   ├── Dockerfile
│   └── pom.xml
│
├── resume-ai-service/
│   ├── ai_service.py
│   ├── main.py
│   ├── models.py
│   └── requirements.txt
│
├── docs/
│   └── images/
│
├── .gitignore
└── README.md
```

> `.env`, virtual environments, API keys, and generated build directories should not be committed to Git.

---

# 💻 Running the Project Locally

## Prerequisites

Install:

- Java 21
- Maven
- Python 3
- Git

A Groq API key is also required for AI inference.

---

## 1. Clone the Repository

```bash
git clone https://github.com/ALDRIN1704/AI-Resume-Analyzer.git
cd AI-Resume-Analyzer
```

---

## 2. Configure FastAPI

Move into the Python service:

```bash
cd resume-ai-service
```

Create a virtual environment:

```bash
python -m venv .venv
```

### Windows

```bash
.venv\Scripts\activate
```

Install dependencies:

```bash
pip install -r requirements.txt
```

Create a `.env` file:

```env
GROQ_API_KEY=your_groq_api_key
```

> Never commit the `.env` file or API key.

Start FastAPI:

```bash
python -m uvicorn main:app --host 127.0.0.1 --port 8000
```

FastAPI should start at:

```text
http://127.0.0.1:8000
```

Health:

```text
http://127.0.0.1:8000/health
```

Swagger:

```text
http://127.0.0.1:8000/docs
```

---

## 3. Start Spring Boot

Open another terminal.

```bash
cd resume-analyzer
```

Run:

```bash
mvn spring-boot:run
```

Alternatively, run:

```text
ResumeAnalyzerApplication.java
```

from Eclipse as a Spring Boot application.

Spring Boot starts at:

```text
http://localhost:8081
```

---

## 4. Open ResumeIQ

Open:

```text
http://localhost:8081/
```

Then:

1. Upload a PDF or DOCX resume.
2. Paste the target job description.
3. Click **Analyze Resume**.
4. Wait for the analysis pipeline to complete.
5. Review the generated report.

---

# ☁️ Deployment

ResumeIQ is deployed as two independent services.

---

## FastAPI Deployment

The Python AI service is deployed on Render.

Configuration:

```text
Root Directory : resume-ai-service
Build Command  : pip install -r requirements.txt
Start Command  : uvicorn main:app --host 0.0.0.0 --port $PORT
```

Environment variable:

```env
GROQ_API_KEY=your_groq_api_key
```

Production URL:

```text
https://ai-resume-analyzer-uazs.onrender.com
```

---

## Spring Boot Deployment

The Java application is containerized using Docker and deployed on Render.

The application reads the AI service URL from:

```env
AI_SERVICE_URL=https://ai-resume-analyzer-uazs.onrender.com
```

Spring Boot uses a dynamic production port:

```properties
server.port=${PORT:8081}
```

Production URL:

```text
https://ai-resume-analyzer-1-g2l2.onrender.com/
```

---

# 🔁 End-to-End Request Flow

```text
User
 |
 | Resume + Job Description
 v
ResumeIQ Frontend
 |
 | POST /api/analyze
 v
Spring Boot
 |
 | Parse PDF / DOCX
 v
Document Parser
 |
 | Extracted Text
 v
Text Cleaning
 |
 | Resume Text + JD
 v
FastAPI /analyze
 |
 | Structured Resume + JD
 v
Java Skill Matching
 |
 | Non-skill Requirements
 v
FastAPI /match-requirements
 |
 | Evidence Matches
 v
Java Scoring Engine
 |
 | Skill Match + ATS Score
 v
FastAPI /generate
 |
 | Suggestions + Cover Letter
 v
Spring Boot
 |
 | AnalysisResult
 v
ResumeIQ Dashboard
```

---

# 🎯 Design Principles

## Deterministic Scoring

The LLM extracts and interprets information, but final scores are calculated using Java business logic.

This makes scoring easier to understand and reproduce.

## Evidence Before Claims

Semantic requirement matches include supporting resume evidence wherever possible.

## No Fabricated Qualifications

Generated suggestions and cover letters are designed to rely on resume-supported information rather than intentionally adding qualifications the candidate has not provided.

## Job-Dependent Evaluation

Different jobs emphasize different requirements.

ResumeIQ extracts requirement priorities from the supplied job description instead of applying one fixed scoring template to every role.

## Separate Skill and Compatibility Scores

Technical skill coverage and broader job compatibility answer different questions, so ResumeIQ displays them independently.

---

# 🛡️ Error Handling

The application handles common failures including:

- Empty resume upload
- Unsupported file formats
- Empty job description
- PDF without readable text
- DOCX without readable text
- AI service communication failure
- Invalid AI response
- API processing errors

Supported resume formats:

```text
.pdf
.docx
```

Maximum upload size:

```text
10 MB
```

---

# ✅ Development Checkpoints

ResumeIQ was developed incrementally, with each major component tested before full integration.

| Phase | Component | Checkpoint |
|---:|---|---|
| 1 | Spring Boot Setup | Backend starts successfully and accepts API requests |
| 2 | Resume Validation | PDF/DOCX files are validated before processing |
| 3 | Document Parsing | PDFBox and Apache POI extract readable resume text |
| 4 | Text Cleaning | Resume text is normalized while preserving technical terms |
| 5 | FastAPI Setup | AI service starts and `/health` responds successfully |
| 6 | AI Extraction | `/analyze` returns structured resume and JD data |
| 7 | Skill Matching | Java identifies matched and missing required skills |
| 8 | Semantic Matching | `/match-requirements` returns resume evidence |
| 9 | Scoring | Java calculates Skill Match and ATS Compatibility |
| 10 | AI Generation | `/generate` returns suggestions and cover letter |
| 11 | Frontend | Analysis results display in the ResumeIQ dashboard |
| 12 | API Integration | Spring Boot communicates with FastAPI |
| 13 | Error Handling | Invalid files and API failures are handled |
| 14 | Deployment | FastAPI and Spring Boot are deployed independently |
| 15 | Production Integration | Deployed Java communicates with deployed FastAPI |
| 16 | End-to-End Test | Resume + JD produces the complete analysis report |

## Final Checkpoint

```text
Resume Upload
     |
     v
Validation & Parsing
     |
     v
Text Cleaning
     |
     v
AI Resume + JD Analysis
     |
     v
Java Skill Matching
     |
     v
Semantic Evidence Matching
     |
     v
Java Scoring
     |
     v
AI Suggestions + Cover Letter
     |
     v
ResumeIQ Dashboard
     |
     v
Complete Analysis
```

---

# 🧪 API Testing

The APIs can be tested independently before running the complete frontend workflow.

## Spring Boot

Test using Postman with multipart form data:

```text
resume          -> File
jobDescription  -> Text
```

Endpoint:

```text
POST http://localhost:8081/api/analyze
```

Production:

```text
POST https://ai-resume-analyzer-1-g2l2.onrender.com/api/analyze
```

---

## FastAPI

The Python endpoints can be tested through Swagger.

Local:

```text
http://127.0.0.1:8000/docs
```

Production:

```text
https://ai-resume-analyzer-uazs.onrender.com/docs
```

The checkpoint-based development process helps isolate document parsing, HTTP communication, AI response validation, semantic matching, and scoring before testing the complete application.

---

# 🔐 Security

Sensitive configuration is stored using environment variables.

The following files/directories should be excluded from Git:

```gitignore
.env
*.env
.venv/
venv/
__pycache__/
*.pyc
target/
```

Never commit API keys directly into source code.

Production environment variables include:

```text
GROQ_API_KEY
AI_SERVICE_URL
PORT
```

---

# ⚠️ Current Limitations

ResumeIQ currently has several limitations:

- Scanned/image-only PDFs do not currently use OCR.
- Semantic analysis depends on an external LLM service.
- Skill matching primarily relies on normalized skill names.
- Skill aliases and technology equivalence can be improved.
- Authentication and user accounts are not implemented.
- Analysis history is not currently persisted.
- The application does not reproduce the proprietary scoring algorithm of a commercial ATS.

---

# 🚀 Future Enhancements

Potential improvements include:

- OCR support for scanned resumes
- Skill synonym and alias normalization
- Embedding-based semantic skill matching
- Resume section quality analysis
- Resume formatting checks
- Configurable scoring weights
- User authentication
- Analysis history
- Database persistence
- Multiple job-description comparison
- Export analysis as PDF
- Automated tests
- CI/CD pipeline
- Rate limiting
- Retry mechanisms
- API resilience improvements

---

# 🎓 What I Learned

Building ResumeIQ involved integrating multiple areas of software engineering:

- Spring Boot REST API development
- Multipart file handling
- PDF and DOCX document processing
- Java service-layer architecture
- Java records and DTO design
- Java HTTP client communication
- Python FastAPI development
- Pydantic request/response validation
- LLM structured JSON extraction
- Prompt design for controlled AI outputs
- Deterministic scoring algorithms
- Semantic requirement matching
- Cross-service error handling
- API testing using Postman and Swagger
- Responsive frontend development
- Docker containerization
- Render cloud deployment
- Environment-variable configuration
- Git and GitHub version control

A major design focus was separating **AI language understanding** from **deterministic application logic** rather than allowing the LLM to control the complete evaluation.

---

# 👨‍💻 Author

**Aldrin Lijo E M**

B.Tech — Information Technology  
St. Joseph's Institute of Technology, Chennai

GitHub:

https://github.com/ALDRIN1704

---

# ResumeIQ

**Built with Java, Spring Boot, FastAPI and AI.**
